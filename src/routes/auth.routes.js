// import express from "express";
// import { signInUsers, signUpUsers, profileHandler } from "../controllers/usersControllers.js";
// import { requireAuth } from "../middlewares/requireAuth.js";
// const Router = express.Router();


// Router.route("/signup")
// .post(signUpUsers);
// Router.route("/login")
// .post(signInUsers);

// Router.route("/profile")
// .get(requireAuth, profileHandler);

// export default Router;